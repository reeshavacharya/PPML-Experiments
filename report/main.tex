\documentclass{article}
\usepackage{graphicx}
\usepackage[margin=0.75in]{geometry}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{amsmath}
\usepackage{hyperref}

\title{Federated Learning for Brain Tumor Segmentation: Experiment Design, Implementation and Analysis}
\author{Reeshav Acharya}
\date{June $24^{th}$, 2026}
\bibliographystyle{IEEEtran}

\begin{document}

\maketitle

\section{Abstract}
This document provides a detailed account of the design, implementation and evaluation of a Federated Learning (FL) experiment on the Federated Tumor Segmentation (FeTS) 2022 dataset using the Swin UNETR model. We describe the reasoning behind each design choice: the dataset partitioning strategy that preserves non-IID properties, the selection of the Swin UNETR architecture, the construction of a fair centralized baseline, and the FL training infrastructure built on Flower. We conclude with a comparative analysis of segmentation performance and resource utilization between the centralized and federated settings.

\section{Goals}
Our goal is to analyze Privacy Enhancing Techniques (PETs) in the medical imaging domain. Because the scope of this topic is broad, we narrow it to a specific comparative analysis: centralized training versus distributed Federated Learning for 3D brain tumor segmentation. Medical data demands the highest standards of privacy, yet training accurate deep learning models typically requires large, diverse datasets. FL offers a paradigm in which institutions collaboratively train a shared model without exchanging raw patient data. We aim to quantify the cost of this privacy guarantee in terms of segmentation accuracy and computational resources.

Our primary task is 3D image segmentation. Segmentation refers to the task of identifying and delineating a region of interest (ROI) in an image. In our setting, the model receives a multi-modal MRI scan of the brain and produces a voxel-wise segmentation mask that identifies tumor sub-regions. During training, each scan is paired with a ground truth mask in which tumor tissue is labeled and healthy tissue is background. Once trained, the model should generalize to produce accurate masks on previously unseen scans.

We measure segmentation performance using the Dice Similarity Coefficient (DSC), which quantifies the spatial overlap between a predicted mask and the ground truth. It is defined as:
\begin{equation}
    \text{DSC} = \frac{2 |P \cap G|}{|P| + |G|}
\end{equation}
where $P$ is the set of predicted voxels and $G$ is the set of ground truth voxels. The score ranges from 0 (no overlap) to 1 (perfect agreement).

This experiment focuses exclusively on FL without Differential Privacy (DP). Subsequent experiments will incorporate DP, Homomorphic Encryption (HE) and Secure Multi-Party Computation (MPC) as additional privacy layers.

\section{Federated Tumor Segmentation Dataset}
The FeTS dataset \cite{pati_federated_2025} has been used extensively in studies on FL for medical imaging \cite{reina_openfl_2022, khan_adaptive_2022, luo_influence_2023, nalawade_federated_2022} and serves as a standard benchmark for FL algorithms and segmentation models. We use the FeTS Challenge 2022 dataset \cite{spyridon_bakas_2022_6622476}, which was designed to evaluate how well federated models generalize when confronted with intrinsically heterogeneous brain tumors, specifically gliomas and glioblastomas.

The dataset comprises clinically acquired, multi-parametric MRI (mpMRI) scans with four sequences: native T1-weighted (T1), contrast-enhanced T1-weighted (T1-Gd), T2-weighted (T2) and T2-FLAIR. These four modalities are stacked as input channels to the model, providing complementary contrast for different tissue types. The dataset builds upon the RSNA-ASNR-MICCAI BraTS 2021 challenge data \cite{baid2023brats2021dataset} and aggregates scans from 23 medical institutions worldwide, including public collections from The Cancer Imaging Archive.

Ground truth segmentation masks label three tumor sub-regions according to the BraTS annotation protocol: Enhancing Tumor (ET, label 4), the peritumoral Edematous tissue (ED, label 2) and the Necrotic tumor core (NCR, label 1). For evaluation, these raw labels are converted into three clinically relevant, potentially overlapping regions:
\begin{itemize}
    \item \textbf{Whole Tumor (WT):} The union of all tumor classes (labels 1, 2, and 4).
    \item \textbf{Tumor Core (TC):} The union of NCR and ET (labels 1 and 4).
    \item \textbf{Enhancing Tumor (ET):} The enhancing region alone (label 4).
\end{itemize}

\paragraph{Training Data} The training portion contains 1,251 patients distributed across the 23 institutions, with ground truth segmentation masks provided for all cases.

\paragraph{Validation and Testing} The official challenge provides 219 validation patients without ground truth; participants must submit predictions to an online evaluation platform. Additionally, federated evaluation in the challenge takes place across 30 independent institutions that are intentionally excluded from training, ensuring evaluation on entirely unseen data distributions. Since we do not have access to this infrastructure, we construct our own test sets from the training data as described in Section~\ref{sec:split}.

\subsection{Data Imbalance and the Non-IID Problem}

The dataset provides two partition files. \texttt{partitioning\_1.csv} assigns each sample to one of the 23 source institutions. \texttt{partitioning\_2.csv} further subdivides the five largest institutions based on median whole-tumor size. In the primary partitioning, Institutions 1 and 18 contribute 511 and 382 samples respectively, together accounting for 71\% of the dataset. Meanwhile, more than 61\% of institutions contribute fewer than 15 samples each \cite{manthe_federated_2024}. This extreme imbalance is representative of real-world medical data federations, where a few large hospitals dominate while many smaller clinics contribute limited data.

\subsection{Addressing the Partitioning Challenge}

Several approaches have been proposed to handle the data imbalance in the FeTS dataset. Manthe et al.\ \cite{manthe_federated_2024} survey these approaches and ultimately resort to shuffling the dataset between institutions to create an Independent and Identically Distributed (IID) setup with synthetic institutions. While this simplifies training, it fundamentally undermines the FL premise: institutions engage in FL precisely because they cannot or will not share their data. Random redistribution eliminates the non-IID property that makes FL challenging and necessary in practice.

\subsection{Adopted Partitioning Strategy}
\label{sec:split}

We adopt a partial clustering strategy that preserves the non-IID property while producing a tractable number of FL clients. The 1,251 training samples are distributed across three clients that reflect the natural institutional hierarchy:

\begin{itemize}
    \item \textbf{Client 1:} Institution 1 --- 511 samples (40.8\%). Represents one large hospital.
    \item \textbf{Client 2:} Institution 18 --- 382 samples (30.5\%). Represents one mid-sized institution.
    \item \textbf{Client 3:} All remaining 21 institutions --- 358 samples (28.6\%). Represents an aggregation of many small clinics, each contributing between 4 and 47 samples.
\end{itemize}

This distribution creates meaningful heterogeneity: Client 1 sees data from a single large institution with consistent acquisition protocols, Client 2 sees data from a different institution with its own characteristics, and Client 3 sees a highly diverse mixture of scans from 21 different sources. The resulting non-IID property arises naturally from inter-institutional differences in scanner hardware, imaging protocols, patient demographics and tumor characteristics.

Since we do not have access to the official held-out test data, each client independently applies an 80\%-10\%-10\% patient-level split for training, validation and testing respectively. This yields the following sample counts:

\begin{table}[h]
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Client} & \textbf{Train} & \textbf{Val} & \textbf{Test} \\
\midrule
Client 1 (Institution 1) & 408 & 51 & 52 \\
Client 2 (Institution 18) & 305 & 38 & 39 \\
Client 3 (Other 21) & 286 & 36 & 36 \\
\midrule
\textbf{Total} & \textbf{999} & \textbf{125} & \textbf{127} \\
\bottomrule
\end{tabular}
\caption{Patient-level data splits per client (80-10-10).}
\label{tab:splits}
\end{table}

\section{Model: Swin UNETR}

The original FeTS 2022 challenge employed a 3D U-Net as the baseline architecture. However, recent advances in transformer-based models for medical image segmentation motivate their evaluation under federated settings. We adopt the Swin UNETR (Swin UNEt TRansformers) model \cite{hatamizadeh_swin_2022}, a transformer-hybrid architecture designed specifically for 3D brain tumor segmentation. Hatamizadeh et al.\ developed Swin UNETR for the BraTS 2021 segmentation challenge --- the same challenge dataset from which the FeTS 2022 data is derived --- and their model ranked among the top-performing approaches in the validation phase.

\subsection{Architecture Overview}

Swin UNETR follows a U-shaped encoder-decoder design but replaces the traditional convolutional encoder with a hierarchical Swin Transformer. The architecture can be decomposed into three components:

\paragraph{Encoder (Swin Transformer)} The encoder reformulates 3D segmentation as a sequence-to-sequence prediction problem. The input volume (4 modalities $\times$ 128 $\times$ 128 $\times$ 128 voxels) is first divided into non-overlapping 3D patches and linearly projected into token embeddings. These tokens are processed by a hierarchy of Swin Transformer stages, each consisting of multiple transformer blocks that compute self-attention within local 3D windows. Between stages, patch merging layers downsample the spatial resolution by a factor of 2 while doubling the feature dimension, producing feature maps at five distinct resolutions.

The key innovation of the Swin Transformer is \textbf{shifted window self-attention}. Standard vision transformers compute global self-attention over all tokens, resulting in quadratic computational complexity with respect to the input size --- prohibitively expensive for 3D volumes. Swin UNETR instead partitions the feature map into non-overlapping local windows and computes self-attention within each window, reducing complexity to linear with respect to the input size. To enable cross-window communication, successive transformer blocks alternate between regular and shifted window configurations, allowing information to flow across window boundaries.

This hierarchical design produces multi-scale feature representations: early stages capture fine-grained local patterns (edges, textures), while deeper stages model long-range contextual dependencies (spatial relationships between tumor sub-regions). This capacity for long-range modeling is the primary advantage over Fully Convolutional Neural Networks (FCNNs), whose limited receptive fields can lead to deficiencies when segmenting tumors of variable size and shape.

\paragraph{Decoder (FCNN)} The decoder follows the standard U-Net paradigm. At each resolution level, the Swin Transformer encoder features are connected to the decoder via skip connections. The decoder progressively upsamples the features through transposed convolutions and residual blocks, recovering the original spatial resolution.

\paragraph{Skip Connections} Skip connections at each of the five resolution levels concatenate encoder features with decoder features. These connections preserve fine-grained spatial details that would otherwise be lost during downsampling, enabling precise boundary delineation of tumor regions.

\subsection{Configuration}

We use the MONAI (Medical Open Network for Artificial Intelligence) implementation of Swin UNETR with the following configuration:
\begin{itemize}
    \item \textbf{Input:} 4-channel 3D patches of size $128 \times 128 \times 128$ (one channel per MRI modality).
    \item \textbf{Output:} 3-channel probability map (WT, TC, ET).
    \item \textbf{Feature size:} 48 (initial embedding dimension).
    \item \textbf{Gradient checkpointing:} Enabled to reduce GPU memory consumption at the cost of additional computation during backpropagation.
    \item \textbf{Spatial dimensions:} 3 (volumetric data).
\end{itemize}

\subsection{Preprocessing Pipeline}

All input volumes undergo a standardized preprocessing pipeline implemented with MONAI transforms:
\begin{enumerate}
    \item \textbf{Orientation and Spacing:} Volumes are reoriented to RAS (Right-Anterior-Superior) convention and resampled to isotropic 1.0\,mm spacing.
    \item \textbf{Intensity Normalization:} Channel-wise z-score normalization is applied to non-zero voxels only, accounting for the background.
    \item \textbf{Foreground Cropping:} Background regions are cropped away to reduce computational waste.
    \item \textbf{Patch Extraction:} During training, random $128^3$ patches are extracted with balanced positive/negative sampling (ensuring patches contain tumor voxels). During validation, volumes are padded to be divisible by 32 and processed whole.
    \item \textbf{Data Augmentation (training only):} Random flips along all three axes, random 90-degree rotations and random intensity shifts.
\end{enumerate}

\section{Baseline Experiment}
\label{sec:baseline}

The centralized baseline simulates the scenario in which all institutional data is pooled into a single dataset. It serves as the upper-bound performance benchmark: if data privacy were not a concern, this is the performance one would achieve by training on all available data at once.

\subsection{Ensuring a Fair Comparison}
\label{sec:fair}

A scientifically valid comparison between centralized and federated training requires that the \textit{only} difference between the two settings is the training paradigm. To guarantee this, we enforce the following constraints:

\begin{enumerate}
    \item \textbf{Identical Data Splits:} The centralized baseline does not create its own random 80/10/10 split over the global dataset. Instead, it precisely aggregates the independent splits computed for each FL client:
    \begin{align*}
        \text{Centralized}_{train} &= \text{Client1}_{train} \cup \text{Client2}_{train} \cup \text{Client3}_{train} \\
        \text{Centralized}_{val} &= \text{Client1}_{val} \cup \text{Client2}_{val} \cup \text{Client3}_{val} \\
        \text{Centralized}_{test} &= \text{Client1}_{test} \cup \text{Client2}_{test} \cup \text{Client3}_{test}
    \end{align*}
    This mathematically guarantees that every patient appears in the same set (train, val or test) in both settings. The random seed for the split is fixed at 42 for reproducibility.

    \item \textbf{Identical Preprocessing:} Both settings use the exact same 3D preprocessing pipeline (orientation, spacing normalization, intensity normalization, cropping and augmentation).

    \item \textbf{Identical Architecture:} Both settings use the same Swin UNETR model with identical hyperparameters (feature size 48, gradient checkpointing enabled).

    \item \textbf{Identical Optimization:} Both settings use the AdamW optimizer with learning rate $3 \times 10^{-4}$ and weight decay $10^{-5}$, and minimize the same DiceCE loss (a combination of Dice loss and Cross-Entropy loss).
\end{enumerate}

\subsection{Training Configuration}

The centralized baseline trains for 100 epochs on the pooled training set of 999 samples with a batch size of 1, necessitated by the large memory footprint of $128^3$ 3D volumes. A Cosine Annealing learning rate schedule decays the learning rate from $3 \times 10^{-4}$ to near zero over the 100 epochs, promoting convergence stability in the later stages of training. The model checkpoint with the highest validation Mean Dice is retained as the best model.

The DiceCE loss combines two complementary objectives: the Dice loss directly optimizes the overlap metric we evaluate on, while the Cross-Entropy loss provides stable gradients early in training when predictions are far from the ground truth. The sigmoid activation is applied to model outputs to produce per-voxel probabilities for each of the three target regions.

Training was conducted on a SLURM-managed HPC cluster node equipped with 2$\times$ NVIDIA L40S GPUs (44.5\,GB VRAM each), 64 CPU cores and 251\,GB RAM. The two GPUs were utilized via PyTorch's DataParallel.

\section{Federated Learning Experiment}
\label{sec:fl}

\subsection{Flower Framework}

We use \textbf{Flower} (flwr), an open-source, framework-agnostic FL platform, to orchestrate federated training. Flower was chosen for several reasons: it provides a clean separation between the FL protocol (server-side aggregation strategy) and the local training logic (client-side model), it supports standard aggregation strategies out of the box, and it communicates over gRPC, enabling deployment across networked machines or within a single node.

In our setup, the Flower server coordinates three clients using the \textbf{Federated Averaging (FedAvg)} strategy. The training proceeds as follows for each of 100 communication rounds:

\begin{enumerate}
    \item The server broadcasts the current global model parameters to all three clients.
    \item Each client loads the received parameters into its local Swin UNETR model and trains for \textbf{one local epoch} on its private dataset partition.
    \item Each client sends its updated model parameters back to the server, along with the number of training examples.
    \item The server computes the new global model as a \textbf{weighted average} of the client parameters, where the weight of each client is proportional to the number of examples it trained on.
    \item The server broadcasts the updated global model back to all clients for evaluation on their local validation sets.
\end{enumerate}

Each client implements Flower's \texttt{NumPyClient} interface, exposing three methods: \texttt{get\_parameters} (extract model weights as NumPy arrays), \texttt{fit} (perform one epoch of local training and return updated weights) and \texttt{evaluate} (compute validation metrics on local data). The server aggregates training losses and validation Dice scores using weighted averaging.

\subsection{Training Configuration}

All FL training hyperparameters mirror the centralized baseline to ensure a fair comparison: AdamW optimizer with learning rate $3 \times 10^{-4}$, weight decay $10^{-5}$, batch size 1, and DiceCE loss. Each client performs one local epoch per communication round, and training runs for 100 rounds total. The best global model (highest weighted-average validation Dice across all clients) is retained.

\subsection{Infrastructure}

The FL experiment was deployed across four separate SLURM-managed HPC nodes, reflecting a realistic distributed training scenario where each participant operates on its own hardware:

\begin{itemize}
    \item \textbf{Server:} \texttt{mdc-1057-13-8} --- CPU-only node (no GPU required for parameter aggregation), 8 CPUs, 32\,GB RAM.
    \item \textbf{Client 1:} \texttt{mdc-1057-18-3} --- 1$\times$ NVIDIA L40S (44.5\,GB VRAM), 16 CPUs, 64\,GB RAM.
    \item \textbf{Client 2:} \texttt{mdc-1057-18-1} --- 1$\times$ NVIDIA L40S (44.5\,GB VRAM), 16 CPUs, 64\,GB RAM.
    \item \textbf{Client 3:} \texttt{mdc-1057-18-4} --- 1$\times$ NVIDIA L40S (44.5\,GB VRAM), 16 CPUs, 64\,GB RAM.
\end{itemize}

Each client ran as an independent SLURM job on a dedicated GPU node, communicating with the server over the cluster's InfiniBand network via gRPC. This multi-node deployment is representative of a real FL federation where institutions train on their own infrastructure and only exchange model parameters. A background \texttt{ResourceMonitor} thread on each client polled system metrics (RAM, VRAM, GPU utilization) every second throughout training.

\section{Results}
\label{sec:results}

\subsection{Segmentation Performance}

Table~\ref{tab:test_results} presents the test-set Dice scores for both the centralized baseline and the federated model evaluated on each client's held-out test partition. The centralized model is evaluated on the union of all client test sets (127 samples), while the FL model is evaluated separately on each client's test split using the best global model from round 92.

\begin{table}[h]
\centering
\begin{tabular}{lcccccc}
\toprule
\textbf{Setting} & \textbf{Test Samples} & \textbf{Test Loss} & \textbf{Dice WT} & \textbf{Dice TC} & \textbf{Dice ET} & \textbf{Mean Dice} \\
\midrule
Centralized (all) & 127 & 0.1364 & 0.9051 & 0.8803 & 0.8256 & 0.8703 \\
\midrule
FL --- Client 1 & 52 & 0.0928 & 0.9280 & 0.9217 & 0.8819 & 0.9105 \\
FL --- Client 2 & 39 & 0.1588 & 0.8726 & 0.8311 & 0.8288 & 0.8442 \\
FL --- Client 3 & 36 & 0.2081 & 0.9170 & 0.8095 & 0.6967 & 0.8078 \\
\midrule
FL --- Weighted Avg & 127 & --- & --- & --- & --- & 0.8610 \\
\bottomrule
\end{tabular}
\caption{Test-set segmentation performance. The FL weighted average is computed by weighting each client's Mean Dice by its number of test samples: $(52 \times 0.9105 + 39 \times 0.8442 + 36 \times 0.8078) / 127 = 0.8610$.}
\label{tab:test_results}
\end{table}

\paragraph{Overall Performance} The weighted-average Mean Dice of the FL model (0.8610) is within 1.1\% of the centralized baseline (0.8703). This modest gap demonstrates that FL with FedAvg can approach centralized performance on non-IID medical imaging data while keeping all patient data within each institution's boundaries.

\paragraph{Per-Client Analysis}
\begin{itemize}
    \item \textbf{Client 1} achieves the highest FL test Dice (0.9105), \textit{exceeding} the centralized baseline (0.8703) by a significant margin. This client holds the largest partition (511 training samples from a single institution). Its homogeneous data distribution means the global model's representations align well with Institution 1's characteristics. The FedAvg weighting further amplifies this advantage: Client 1 contributes the most training examples, so its parameter updates receive the highest weight during aggregation.

    \item \textbf{Client 2} achieves a Mean Dice of 0.8442, below the centralized baseline by 2.6 percentage points. Institution 18 contributes the second-largest share of data, but its scans exhibit different acquisition characteristics than Institution 1. During FedAvg, Client 1's larger update weight can pull the global model slightly away from Institution 18's optimal parameters, diluting institution-specific features.

    \item \textbf{Client 3} shows the lowest Mean Dice (0.8078), driven primarily by a significantly lower Enhancing Tumor Dice (0.6967 vs.\ 0.8256 centralized). This client aggregates 21 small institutions, each contributing only 4--47 samples. The extreme heterogeneity within this client --- different scanners, protocols and patient demographics across 21 sources --- makes it the hardest partition for the global model to fit. The ET region is typically the smallest and most variable tumor sub-region, making it particularly sensitive to distribution shifts. With only 286 training samples spread across 21 distinct sources, no single source provides enough data to robustly capture ET boundaries, and the global model's compromise across all clients hurts this already-challenging sub-region the most.
\end{itemize}

\subsection{Training Dynamics}

Table~\ref{tab:training} summarizes the training progression for both settings.

\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\toprule
\textbf{Setting} & \textbf{Best Epoch/Round} & \textbf{Best Val Dice} & \textbf{Final Val Dice} & \textbf{Total Duration} \\
\midrule
Centralized & Epoch 78 & 0.8966 & 0.8949 & $\sim$17 hours \\
FL (global) & Round 92 & 0.8888 & 0.8842 & $\sim$11 hours \\
\bottomrule
\end{tabular}
\caption{Training dynamics comparison.}
\label{tab:training}
\end{table}

The centralized model achieved its best validation Dice of 0.8966 at epoch 78, while the FL model peaked at 0.8888 at round 92. Both models exhibit convergence stability in later stages, with minimal fluctuation in validation Dice after approximately round/epoch 60. The FL model's delayed peak (round 92 vs.\ epoch 78) is expected: each FL round involves only one local epoch followed by parameter averaging, and the aggregation step introduces noise that the model must absorb over additional rounds.

The FL experiment completed in approximately 11 hours --- significantly faster than the 17-hour centralized run --- because each client trains on only its own partition (286--408 samples) rather than the full pooled dataset (999 samples). All three clients train simultaneously on separate nodes, so the wall-clock time per round is determined by the slowest client, which has at most 408 samples. The parameter serialization, gRPC transfer and server-side aggregation overhead is negligible relative to the forward and backward passes on 3D volumes.

\subsection{Per-Client Training Behavior}

Analysis of the per-client validation Dice over 100 rounds reveals distinct convergence patterns:

\begin{table}[h]
\centering
\begin{tabular}{lccccc}
\toprule
\textbf{Client} & \textbf{Train Samples} & \textbf{Round 1 Val Dice} & \textbf{Best Val Dice} & \textbf{Best Round} & \textbf{Round 100 Val Dice} \\
\midrule
Client 1 & 408 & 0.6895 & 0.9123 & 93 & 0.9078 \\
Client 2 & 305 & 0.6526 & 0.9020 & 92 & 0.8847 \\
Client 3 & 286 & 0.7319 & 0.8625 & 86 & 0.8504 \\
\bottomrule
\end{tabular}
\caption{Per-client FL training progression.}
\label{tab:client_train}
\end{table}

All three clients start from the same randomly initialized global model (Dice 0.65--0.73) and converge steadily, with Client 3 reaching its local peak earliest at round 86 while Clients 1 and 2 peak later at rounds 92--93. Client 3's earlier saturation and lower ceiling reflect the challenge of fitting a single global model to data spanning 21 heterogeneous institutions. Client 2 starts with the lowest initial Dice (0.6526) but converges effectively, reaching a best validation Dice of 0.9020. The gap between each client's validation Dice and its test Dice is consistent (2--5 percentage points), indicating that the validation and test sets are representative of each other within each partition.

\subsection{Resource Utilization}

Table~\ref{tab:resources} presents the resource consumption measured during training. In the multi-node FL deployment, each client ran on its own dedicated GPU node, enabling direct comparison of per-client resource usage against the centralized baseline.

\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\toprule
\textbf{Metric} & \textbf{Centralized} & \textbf{FL Client 1} & \textbf{FL Client 2} & \textbf{FL Client 3} \\
\midrule
Node & mdc-1057-18-1 & mdc-1057-18-3 & mdc-1057-18-1 & mdc-1057-18-4 \\
GPUs & 2$\times$ L40S & 1$\times$ L40S & 1$\times$ L40S & 1$\times$ L40S \\
Peak VRAM (MB) & 26,719 & 26,387 & 21,731 & 21,731 \\
Peak RAM (MB) & 11,578 & 15,873 & 18,128 & 21,349 \\
GPU Utilization & 100\% & 100\% & 100\% & 100\% \\
Training Duration & $\sim$17 hours & \multicolumn{3}{c}{$\sim$11 hours (all clients, simultaneous)} \\
\bottomrule
\end{tabular}
\caption{Resource utilization comparison across dedicated nodes.}
\label{tab:resources}
\end{table}

\paragraph{GPU Memory} The centralized baseline used 26.7\,GB of VRAM across two L40S GPUs via DataParallel. Each FL client used a single L40S GPU, with Client 1 consuming a comparable 26.4\,GB and Clients 2 and 3 consuming 21.7\,GB each. The per-client VRAM usage is proportional to the local dataset size: Client 1's larger partition (408 training samples) requires more memory for the data pipeline, while Clients 2 and 3 have smaller partitions. These measurements confirm that each FL client's GPU memory footprint is comparable to a single GPU's share in the centralized DataParallel setup.

\paragraph{System RAM} FL clients consumed more system RAM (15.9--21.3\,GB) than the centralized baseline (11.6\,GB). This is because each client independently loads and preprocesses its 3D volumes in separate DataLoader worker processes, whereas the centralized baseline shares a single set of workers. Client 3's higher RAM usage (21.3\,GB) correlates with its more heterogeneous data from 21 institutions, which may produce more varied volume sizes after preprocessing.

\paragraph{Training Time} The FL experiment completed in approximately 11 hours, 35\% faster than the 17-hour centralized run. This speedup arises because each FL round involves one local epoch on a partition of 286--408 samples (executed in parallel across three nodes), compared to one centralized epoch over 999 samples on a single node. The communication overhead --- serializing, transferring and deserializing the Swin UNETR's $\sim$62M parameters ($\sim$248\,MB) per round via gRPC --- is negligible relative to the minutes-long training step.

\paragraph{Total Resource Consumption} While each FL client uses fewer resources than the centralized node, the aggregate resource footprint is higher: 3 GPU nodes and 1 CPU node versus a single 2-GPU node. This is the fundamental trade-off of FL --- privacy preservation comes at the cost of distributed hardware requirements. In a real-world medical federation, however, these resources already exist at each institution; the FL overhead is limited to the server node and network bandwidth.

\subsection{Summary}

The experimental results lead to three conclusions:

\begin{enumerate}
    \item \textbf{FL achieves competitive performance.} The weighted-average test Dice of the FL model (0.8610) is within 1.1\% of the centralized baseline (0.8703), confirming that FedAvg can effectively aggregate knowledge from non-IID medical imaging data with only a modest reduction in segmentation quality. This gap is notably smaller than the variance between individual clients.

    \item \textbf{Performance varies across clients according to data characteristics.} Client 1 (large, homogeneous institution) exceeds the centralized baseline with a Mean Dice of 0.9105. Client 2 (mid-sized institution) achieves 0.8442, a moderate gap. Client 3 (21 small heterogeneous clinics) shows the largest deficit at 0.8078, driven almost entirely by the challenging Enhancing Tumor sub-region (0.6967 vs.\ 0.8256). This gradient directly reflects the non-IID severity: the more heterogeneous and data-scarce a client's partition, the less a global model serves its specific distribution. This motivates future work on personalized FL strategies that adapt the global model to each client's local characteristics.

    \item \textbf{FL is faster but requires more total hardware.} The multi-node FL experiment completed in $\sim$11 hours versus $\sim$17 hours for centralized training, a 35\% wall-clock speedup from parallelism. However, FL consumed 3 GPU nodes plus a server node compared to a single 2-GPU node. Per-client resource usage (VRAM, GPU utilization) was comparable to the centralized per-GPU baseline, confirming that the Flower framework and gRPC communication introduce negligible computational overhead. The primary cost of FL is not computation but the coordination of distributed infrastructure.
\end{enumerate}

\bibliography{references}

\end{document}
